<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context.xsd
       http://www.springframework.org/schema/mvc
       http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- 1. 开启注解扫描 (Controller, Service, Dao) -->
    <context:component-scan base-package="com.example.thing"/>

    <!-- 2. 开启 Spring MVC 注解驱动 -->
    <mvc:annotation-driven/>

    <!-- 3. 处理静态资源 (防止 css/js 被拦截) -->
    <mvc:default-servlet-handler/>

    <!-- 4. 视图解析器 (JSP) -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/jsp/"/>
        <property name="suffix" value=".jsp"/>
    </bean>

    <!-- 5. 配置数据源 (连接池) - 直接写死账号密码 -->
    <bean id="dataSource" class="com.alibaba.druid.pool.DruidDataSource" init-method="init" destroy-method="close">
        <property name="driverClassName" value="com.mysql.cj.jdbc.Driver"/>

        <!-- URL: 这里的 127.0.0.1 指的是服务器自己 -->
        <property name="url" value="jdbc:mysql://127.0.0.1:3306/secondhand_db?useUnicode=true&amp;characterEncoding=utf-8&amp;serverTimezone=GMT%2B8&amp;useSSL=false"/>

        <property name="username" value="root"/>

        <!-- 【重点】这里填你 Linux 服务器的密码！ -->
        <property name="password" value="Xyz050305"/>
    </bean>

    <!-- 6. 配置 JdbcTemplate (Spring 提供的 JDBC 工具) -->
    <bean id="jdbcTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
        <property name="dataSource" ref="dataSource"/>
    </bean>

</beans>